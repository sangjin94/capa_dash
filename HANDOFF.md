# 인수인계 — 센터 CAPA 관리 시스템 (capa_dash)

한익스프레스 물류센터의 **보관 CAPA를 도면 기반으로 산출하고, WMS(gaon) 실재고를 얹어
3D로 확인하는 사내 도구**입니다. 이 문서만 보고 이어서 개발·배포할 수 있게 정리했습니다.

- 저장소 : https://github.com/sangjin94/capa_dash
- 기술 : 순수 HTML/CSS/JS (빌드 없음) + Python 표준 라이브러리 서버 (의존 패키지 없음)
- 현재 : **로컬 실행만 하고 있고 배포는 안 된 상태.** 남이천1센터 3개 층 도면 작업 완료

---

## 1. 5분 안에 띄우기

```bash
git clone https://github.com/sangjin94/capa_dash.git
cd capa_dash
```

Windows 는 **`start_server.bat` 더블클릭**. 그 외에는:

```bash
python server/serve.py          # http://localhost:5180
```

Python 3.9+ 만 있으면 됩니다. `pip install` 필요 없습니다.
빌드 과정이 없어서 파일을 고치고 브라우저를 새로고침하면 바로 반영됩니다.
(캐시가 남으면 `index.html` 끝의 `app.js?v=NN` 숫자를 올리세요. 지금까지 그렇게 관리했습니다.)

---

## 2. 무엇을 하는 도구인가

| 화면 | 내용 |
|---|---|
| 대시보드 | 센터별 전체/사용/여유 CAPA, 분류별 현황, 점유 고객사 |
| 센터별 입력 | 수기 CAPA 입력 (도면이 없는 센터용 보조 + 도면값과 대조) |
| 화주사 분석 | 월별 보관량 추이 (WMS 엑셀 업로드) |
| 마스터 관리 | 센터·분류·화주 마스터, 센터-화주 맵핑 |
| **3D 점유도** | 도면 위 랙 배치 편집 + 3D 시각화. **이 도구의 핵심** |
| **재고 관리** | gaon 재고 ↔ 도면 랙 맵핑 점검, 미전산재고 관리 |

### CAPA 산출 규칙 (중요)

```
격자 1칸 = 파렛트 1자리    (도면을 216 × 126 격자로 본다)

파렛트랙 : 베이 × 단                        = PLT
평치/벌크 : 면적(칸) × 적재단수 × 유효적재율  = PLT   (기본 2단 · 80%)
경량랙   : 베이 × 단                        = 로케이션 수
           로케이션 × 환산계수               = PLT   (기본 0.2, 요소별 조정)

사용 CAPA = gaon 실재고 PLT + 미전산재고 PLT
```

전체 CAPA는 세 가지를 PLT로 합산하되, **경량랙은 로케이션 수를 별도 지표로도 보여줍니다**
(파렛트와 단위가 달라 섞으면 뜻이 흐려짐). 3D 좌상단에 형태별 점유가 따로 표시됩니다.

### 재고 맵핑 규칙

gaon 셀코드 `존-랙열-베이-단` (예 `02-01-05-30`, 단은 10/20/30/40) 을 도면 랙과 대조합니다.

- 랙의 **셀 접두**(`02-01`)가 기준
- **단별 접두 예외** 지원 — 현장에 1단(피킹)과 2~4단의 랙열 번호가 다른 랙이 있어서,
  랙 속성에 `1=05-01` 형식으로 예외를 넣을 수 있습니다
- 판정 결과는 `맵핑됨 / 미맵핑 / 범위 초과` 세 가지

---

## 3. 코드 구조

```
index.html          화면 구조 전부 (SPA, 뷰를 class="view active" 로 전환)
app.js              앱 로직 전부 (약 7,000줄, 섹션 주석으로 구분)
styles.css          디자인
assets/centers/     센터 도면 PNG (도면 좌표 추출의 원본)
assets/vendor/      three.js, OrbitControls, pdf.js, xlsx  (CDN 안 씀 — 사내망 고려)

server/serve.py         정적 제공 + gaon 중계 + 공유 저장소 API
server/gaon_client.py   gaon(Nexacro) 연동 — 로그인·재고조회·응답 파싱
server/store.py         공유 저장소 (키 단위 + 낙관적 잠금)
server/restore_backup.py 배치 백업 JSON 복원 도구
server/README.md        서버·배포 상세 문서 ← 배포는 이 문서를 보세요
```

`app.js` 의 주요 구간 (검색해서 찾으세요):

| 찾을 문자열 | 내용 |
|---|---|
| `DEFAULT_RACK_LAYOUTS` | 도면에서 추출한 기본 랙 배치 (남이천1센터 3개 층) |
| `TWIN_ELEMENT_TYPES` | 요소 타입 정의 (랙·경량랙·평치·기둥·벽 등) |
| `floorRackCapa` | 층 단위 CAPA 집계 |
| `floorCapaBreakdown` | 보관 형태별 용량·점유 |
| `analyzeInventory` | 재고 ↔ 랙 맵핑 판정 |
| `rackPrefixForLevel` | 단별 접두 |
| `render3DTwin` / `buildTwin*` | 3D 렌더 |
| `initSync` / `syncPush` / `syncPull` | 공유 저장소 동기화 |

---

## 4. 데이터가 어디 있는지 — **가장 조심할 부분**

**앱 데이터는 기본적으로 브라우저 localStorage 에만 있습니다.** (키 `hx-center-capa-v2`)
저장소나 서버에 없습니다. 도면 배치·재고·미전산이 전부 여기 들어 있습니다.

### 인수인계 시 데이터 넘기는 법

1. 넘기는 쪽: 앱에서 `3D 점유도 → 랙 배치 편집 → 배치 관리 → 💾 백업 저장`
2. 받는 쪽: 같은 자리에서 `📂 백업 불러오기` 로 그 JSON 을 읽기

서버 공유 저장소를 쓰는 경우에는:

```bash
python server/restore_backup.py "경로/capa_배치백업.json" --dry-run   # 먼저 확인
python server/restore_backup.py "경로/capa_배치백업.json"
```

### ⚠️ 과거 사고 이력 (반복 금지)

공유 저장소(`SHARE=1`)를 켜면 **접속 시 서버 내용이 브라우저를 덮어씁니다.**
개발 중 `server/data` 를 지웠다 만들면서, 테스트 브라우저가 올린 기본 배치가
실제 편집본을 밀어낸 사고가 있었습니다.

그래서 지금은 이렇게 막아뒀습니다.

- **`SHARE` 는 기본 꺼짐.** 꺼져 있으면 `/api/store*` 가 404 를 돌려주고 앱은 브라우저 저장만 씁니다
- 테스트할 때는 `CAPA_DATA_DIR=/tmp/xxx` 로 **실데이터 폴더를 반드시 분리**하세요
- 저장 직전 스냅샷이 `server/data/backups/` 에 30개까지 자동 순환 보관됩니다

---

## 5. 지금까지 한 것

- 도면 기반 CAPA 자동 산출 (수기 입력 합산 → 도면 실측 기반으로 전환)
- gaon WMS 재고 연동 — 화주별 셀 재고 조회 → PLT 환산 → 층·분류·화주별 배분
- 재고 ↔ 도면 맵핑 점검 화면 (미맵핑·범위초과 진단, 랙열 단위 즉시 배정)
- 미전산재고 관리 (gaon 에 없는 실물 재고 → 사용 CAPA 에 합산)
- 3D 디지털 트윈 (화주 색상, 요소별 표시/이름표 제어, 형태별 점유)
- 요소 타입 : 파렛트랙 · **경량랙** · **평치/벌크** · 사선 벽 · 기둥 · 사무실 · 도크 등
- 랙 보관 분류(일반/보세/벌크/위험물/상온/저온) → 분류별 CAPA 자동 산출
- 편집 모드 4종 (고정/값편집/이동/생성) — 실수로 요소가 생기는 문제 대응
- 공유 저장소 (키 단위 + 낙관적 잠금, 브라우저 2대로 동시 작업 검증 완료)
- 남이천1센터 도면 작업 : 지하1층 237요소 / 지상2층 196 / 지상4층 197

---

## 6. 남은 일 (우선순위 순)

### 1) gaon 로그인 검증 — **가장 중요, 유일한 미검증 항목**

기능은 다 구현돼 있지만 **실계정으로 성공한 적이 한 번도 없습니다.**
현재 로그인 요청은 이렇게 보냅니다 (`server/gaon_client.py:login_body`).

```
POST https://gaon.hanex.co.kr/hanex/ex/login.do
  sCompanyCd = 100        ← GAON_COMPANY 로 변경 가능
  sUserId    = 사번
  sUserPwd   = 비밀번호   ← 평문 전송
  sDomain=ko_KR / sWebViewType=desktop / __tcsFormId=frmLogin
```

의심 후보 (순서대로 확인):

1. **회사코드**가 `100` 이 아닐 수 있음
2. **비밀번호 전송 방식** — gaon 로그인 화면이 브라우저에서 암호화해 보낸다면 평문으로는 실패합니다
3. 사번/비번 오타

**확인 방법** : 크롬에서 gaon 에 직접 로그인하면서 `F12 → Network` 로 `login.do` 요청의
Payload 를 확인하세요. **비밀번호 값은 지우고** 파라미터 이름과 값의 형태(평문/해시)만 보면 됩니다.
서버 콘솔에 실패 사유가 찍히도록 로그를 넣어뒀습니다 (비밀번호는 안 찍힘).

> 이 조회는 gaon 에 감사 로그(사번·IP)를 남깁니다. 자동화 전 **전산팀 승인**을 권합니다.

### 2) 배포

`server/README.md` 의 **Lightsail 배포** 절을 그대로 따라가면 됩니다 (systemd 유닛 포함).
배포 시 반드시:

- **HTTPS 필수** — 안 붙이면 접속 암호와 gaon 사번·비밀번호가 평문으로 오갑니다.
  nginx + Let's Encrypt 로 감싸고 5180 은 외부에 직접 열지 마세요
- `SHARE=1` + `APP_PASSWORD` 설정 (공유 저장소를 쓸 경우)
- `GAON_ID`/`GAON_PW` 는 서버에 두지 말고 각자 앱에서 로그인하게 두는 편이 안전
- `server/data/` 정기 백업

### 3) 나머지 8개 센터

남이천1센터 외에는 도면 배치가 없어 CAPA 가 0 입니다.
층별 도면(PDF/PNG)을 확보해 `assets/centers/` 에 넣고 배치를 그려야 합니다.
도면에서 좌표를 자동 추출한 방법은 사선 벽 작업 때 쓴 방식(검정 선 마스킹 → Hough 변환 →
기존 기둥 좌표와 대조 검증)을 참고하세요.

### 4) 현장 기준값 확정

- **경량랙 PLT 환산계수** — 지금 기본 0.2 는 임의값입니다. 현장 기준으로 확정 필요
- **평치 유효 적재율** — 기본 80%
- 1단과 2~4단의 랙열 번호가 다른 랙 목록 (단별 접두 예외 입력 대상)

### 5) 개선 여지

- 재고 스냅샷 이력 비교 (어제 대비 증감) — 현재는 센터당 스냅샷 1개만 보관
- 센터별 입력 탭 정리 — 도면이 다 들어오면 사실상 불필요해짐

---

## 7. 개발할 때 알아둘 것

- **빌드 없음.** 파일 수정 → 새로고침. `index.html` 의 `?v=NN` 을 올려 캐시를 무효화
- **의존성 없음.** 외부 라이브러리는 `assets/vendor/` 에 파일로 두고 CDN 을 쓰지 않습니다 (사내망)
- `app.js` 는 클래식 스크립트라 **최상단에서 실행되는 코드가 아래쪽 `let` 을 참조하면
  TDZ 로 앱 전체가 죽습니다.** 실제로 두 번 겪었습니다. 초기화 중 호출되는 함수가 쓰는
  변수는 파일 앞쪽에 선언하세요
- 한국어 Windows 콘솔(cp949)에서 print 하나로 서버가 죽을 수 있어 stdout 을 UTF-8 로
  재설정해 뒀습니다
- 요소 좌표는 216 × 126 격자 기준. 도면 이미지는 `object-fit: contain` 으로 얹혀 있어
  픽셀↔격자 변환 시 레터박스 여백을 고려해야 합니다

---

## 8. 넘겨받는 분이 먼저 할 일

1. 저장소 접근 권한 받기 (GitHub collaborator)
2. 클론 → `start_server.bat` 또는 `python server/serve.py` 로 실행 확인
3. **배치 백업 JSON 을 받아서** `배치 관리 → 백업 불러오기` 로 도면 데이터 적재
4. `server/README.md` 정독 (배포 절차·주의사항)
5. gaon 로그인 검증부터 착수 (위 6-1)
